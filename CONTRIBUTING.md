# 🤝 المساهمة في مشروع ساري | Contributing to Sari

نرحب بمساهماتكم! هذا الدليل سيساعدك على البدء.

## 📋 جدول المحتويات

- [قواعد السلوك](#قواعد-السلوك)
- [كيف يمكنني المساهمة؟](#كيف-يمكنني-المساهمة)
- [عملية التطوير](#عملية-التطوير)
- [معايير الكود](#معايير-الكود)
- [رسائل الـ Commit](#رسائل-الـ-commit)
- [Pull Requests](#pull-requests)

## 📜 قواعد السلوك

### تعهدنا
نحن ملتزمون بتوفير بيئة ترحيبية وشاملة للجميع.

### المعايير
- استخدام لغة ترحيبية وشاملة
- احترام وجهات النظر والخبرات المختلفة
- قبول النقد البناء بلطف
- التركيز على ما هو أفضل للمجتمع

## 🎯 كيف يمكنني المساهمة؟

### الإبلاغ عن الأخطاء
قبل إنشاء تقرير خطأ:
- تحقق من قائمة الـ Issues الموجودة
- تأكد من أنك تستخدم أحدث إصدار
- جمع معلومات حول الخطأ

عند إنشاء تقرير خطأ، قدم:
- **عنوان واضح ووصفي**
- **خطوات إعادة إنتاج المشكلة**
- **السلوك المتوقع والفعلي**
- **لقطات شاشة** (إن أمكن)
- **البيئة**: نظام التشغيل، إصدار Node.js، إلخ

### اقتراح ميزات جديدة
قبل اقتراح ميزة:
- تحقق من أنها غير موجودة بالفعل
- ابحث في Issues المغلقة

عند اقتراح ميزة:
- **وصف واضح للميزة**
- **حالات الاستخدام**
- **الفوائد المتوقعة**
- **التحديات المحتملة**

### أول مساهمة
ابحث عن Issues بالعلامات:
- `good first issue` - مناسبة للمبتدئين
- `help wanted` - نحتاج مساعدة
- `documentation` - تحسين التوثيق

## 💻 عملية التطوير

### 1. Fork المشروع
```bash
# انقر على زر Fork في GitHub
# ثم استنسخ نسختك
git clone https://github.com/YOUR_USERNAME/sari.git
cd sari
```

### 2. إعداد البيئة
```bash
# تثبيت الحزم
pnpm install

# إعداد قاعدة البيانات
pnpm db:push

# تشغيل المشروع
pnpm dev
```

### 3. إنشاء Branch جديد
```bash
# استخدم اسم وصفي
git checkout -b feature/amazing-feature
# أو
git checkout -b fix/bug-description
```

### 4. إجراء التغييرات
- اكتب كود نظيف وواضح
- اتبع معايير الكود (انظر أدناه)
- أضف اختبارات للميزات الجديدة
- حدّث التوثيق إذا لزم الأمر

### 5. اختبار التغييرات
```bash
# تشغيل جميع الاختبارات
pnpm test

# اختبار يدوي في المتصفح
pnpm dev
```

### 6. Commit التغييرات
```bash
git add .
git commit -m "feat: إضافة ميزة رائعة"
```

### 7. Push إلى GitHub
```bash
git push origin feature/amazing-feature
```

### 8. فتح Pull Request
- اذهب إلى صفحة المشروع على GitHub
- اضغط "New Pull Request"
- اختر branch الخاص بك
- املأ قالب PR

## 📏 معايير الكود

### TypeScript
```typescript
// ✅ جيد
interface User {
  id: number;
  name: string;
  email: string;
}

function getUserById(id: number): User | null {
  // implementation
}

// ❌ سيء
function getUser(id: any): any {
  // implementation
}
```

### React Components
```tsx
// ✅ جيد
export function ProductCard({ product }: { product: Product }) {
  return (
    <div className="rounded-lg border p-4">
      <h3 className="text-lg font-semibold">{product.name}</h3>
      <p className="text-muted-foreground">{product.price} ريال</p>
    </div>
  );
}

// ❌ سيء
export function ProductCard(props: any) {
  return <div style={{ padding: "16px" }}>{props.name}</div>;
}
```

### tRPC Procedures
```typescript
// ✅ جيد
products: {
  list: protectedProcedure
    .input(z.object({ merchantId: z.number() }))
    .query(async ({ input, ctx }) => {
      return await getProductsByMerchant(input.merchantId);
    }),
}

// ❌ سيء
products: {
  list: publicProcedure
    .query(async () => {
      return db.select().from(products);
    }),
}
```

### قواعد عامة
- استخدم **TypeScript** دائماً (لا `any`)
- استخدم **Tailwind CSS** للتنسيق (لا inline styles)
- استخدم **shadcn/ui** للمكونات
- اتبع **معايير RTL** للعربية
- أضف **تعليقات** للكود المعقد
- اكتب **اختبارات** للميزات الجديدة

## 📝 رسائل الـ Commit

نستخدم [Conventional Commits](https://www.conventionalcommits.org/):

### التنسيق
```
<type>(<scope>): <subject>

<body>

<footer>
```

### الأنواع
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تحديث التوثيق
- `style`: تنسيق الكود (لا يؤثر على المنطق)
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة اختبارات
- `chore`: مهام صيانة

### أمثلة
```bash
feat(products): إضافة ميزة رفع CSV
fix(whatsapp): إصلاح مشكلة الاتصال بـ Green API
docs(readme): تحديث تعليمات التثبيت
test(ai): إضافة اختبارات لساري AI
```

## 🔄 Pull Requests

### قبل فتح PR
- ✅ جميع الاختبارات تمر
- ✅ الكود يتبع المعايير
- ✅ التوثيق محدث
- ✅ لا توجد تعارضات مع main

### قالب PR
```markdown
## الوصف
وصف موجز للتغييرات

## نوع التغيير
- [ ] إصلاح خطأ
- [ ] ميزة جديدة
- [ ] تحسين الأداء
- [ ] تحديث التوثيق

## الاختبارات
- [ ] تمت إضافة اختبارات جديدة
- [ ] جميع الاختبارات تمر
- [ ] تم الاختبار يدوياً

## لقطات الشاشة
(إن وجدت)

## ملاحظات إضافية
أي معلومات أخرى مهمة
```

### عملية المراجعة
1. سيقوم أحد المشرفين بمراجعة PR
2. قد يطلب تعديلات
3. بعد الموافقة، سيتم دمج PR
4. سيتم إغلاق Issue المرتبط تلقائياً

## 🧪 الاختبارات

### كتابة اختبارات
```typescript
// server/products.test.ts
import { describe, expect, it } from "vitest";
import { appRouter } from "./routers";

describe("products.create", () => {
  it("should create a new product", async () => {
    const caller = appRouter.createCaller(merchantCtx);
    const product = await caller.products.create({
      name: "منتج تجريبي",
      price: 99.99,
      stock: 10,
    });
    
    expect(product).toBeDefined();
    expect(product.name).toBe("منتج تجريبي");
  });
});
```

### تشغيل الاختبارات
```bash
# جميع الاختبارات
pnpm test

# اختبار محدد
pnpm test server/products.test.ts

# مع التغطية
pnpm test --coverage
```

## 📚 الموارد

- [React Documentation](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [tRPC Documentation](https://trpc.io)
- [Tailwind CSS](https://tailwindcss.com)
- [shadcn/ui](https://ui.shadcn.com)
- [Drizzle ORM](https://orm.drizzle.team)

## 💬 الحصول على المساعدة

- 📧 البريد: support@sary.live
- 💬 WhatsApp: +966501898700
- 🌐 الموقع: https://sary.live
- 📖 التوثيق: راجع مجلد `/docs`

## 🙏 شكراً لك!

كل مساهمة، مهما كانت صغيرة، تساعد في تحسين ساري. شكراً لوقتك وجهدك! ❤️

---

**صُنع بكل ❤️ في المملكة العربية السعودية 🇸🇦**
