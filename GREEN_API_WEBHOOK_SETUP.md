# إعداد Green API Webhook

هذا الدليل يشرح كيفية ربط Green API مع ساري AI للرد التلقائي على رسائل WhatsApp.

---

## 📋 المتطلبات

1. ✅ حساب Green API نشط
2. ✅ Instance ID و API Token من Green API
3. ✅ المشروع منشور على Manus (أو أي domain آخر)

---

## 🔧 خطوات الإعداد

### 1. الحصول على Webhook URL

بعد نشر المشروع، ستحصل على URL مثل:
```
https://your-domain.manus.space/api/webhook/whatsapp
```

**مهم:** استبدل `your-domain` بالـ domain الفعلي لمشروعك.

---

### 2. تفعيل Webhook في Green API Console

1. افتح [Green API Console](https://console.green-api.com/)
2. اختر الـ Instance الخاص بك
3. اذهب إلى **Settings** → **Webhooks**
4. فعّل الخيارات التالية:
   - ✅ **Incoming messages** (الرسائل الواردة)
   - ✅ **Incoming message received** (استقبال الرسائل)
5. أضف Webhook URL:
   ```
   https://your-domain.manus.space/api/webhook/whatsapp
   ```
6. احفظ الإعدادات

---

### 3. اختبار الاتصال

1. أرسل رسالة نصية إلى رقم WhatsApp المربوط بـ Green API
2. يجب أن ترد ساري تلقائياً خلال 1-3 ثواني
3. تحقق من صفحة **المحادثات** في لوحة التحكم

---

### 4. اختبار الرسائل الصوتية

1. أرسل رسالة صوتية
2. ساري ستحول الصوت إلى نص باستخدام Whisper
3. ثم ترد بناءً على محتوى الرسالة

**ملاحظة:** الرسائل الصوتية تخضع لحدود الباقة.

---

## 🔍 استكشاف الأخطاء

### لا تصل الرسائل؟

1. **تحقق من Webhook URL:**
   - افتح: `https://your-domain.manus.space/api/webhook/health`
   - يجب أن ترى: `{"status":"ok","timestamp":"...","service":"Sari AI Webhook"}`

2. **تحقق من Green API Instance:**
   - تأكد أن الـ Instance نشط (status: active)
   - تأكد من صلاحية API Token

3. **تحقق من Logs:**
   - افتح Console في المتصفح
   - ابحث عن `[Webhook]` في الـ logs

### ساري لا ترد؟

1. **تحقق من OPENAI_API_KEY:**
   - تأكد أن المفتاح صحيح وله رصيد
   - اذهب إلى Settings → Secrets

2. **تحقق من حدود الباقة:**
   - للرسائل الصوتية، تحقق من حد الاستخدام الشهري

3. **تحقق من قاعدة البيانات:**
   - افتح صفحة **المحادثات**
   - يجب أن ترى الرسائل الواردة

---

## 📊 مراقبة الأداء

### Webhook Health Check

```bash
curl https://your-domain.manus.space/api/webhook/health
```

**النتيجة المتوقعة:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-07T04:30:00.000Z",
  "service": "Sari AI Webhook"
}
```

### تتبع المحادثات

1. اذهب إلى **المحادثات** في لوحة التحكم
2. ستجد جميع الرسائل الواردة والصادرة
3. يمكنك رؤية:
   - وقت الرسالة
   - محتوى الرسالة
   - رد ساري AI
   - حالة الإرسال

---

## 🎯 نصائح للأداء الأفضل

1. **استخدم رسائل واضحة:**
   - ساري تفهم اللهجة السعودية والإنجليزية
   - كلما كانت الرسالة أوضح، كان الرد أدق

2. **الرسائل الصوتية:**
   - تحدث بوضوح
   - تجنب الضوضاء في الخلفية
   - الرسائل القصيرة (< 1 دقيقة) أفضل

3. **المحادثات المتعددة:**
   - ساري تحتفظ بسياق المحادثة
   - كل عميل له محادثة منفصلة

---

## 🔒 الأمان

- ✅ جميع الرسائل مشفرة عبر HTTPS
- ✅ Webhook يتحقق من Instance ID
- ✅ رسائل المجموعات يتم تجاهلها تلقائياً
- ✅ معالجة الأخطاء تمنع تسريب المعلومات

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم **استكشاف الأخطاء** أعلاه
2. تحقق من الـ logs في Console
3. تواصل مع الدعم الفني

---

**آخر تحديث:** 7 يناير 2025
