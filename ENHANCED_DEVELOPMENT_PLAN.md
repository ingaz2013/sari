# 🚀 خطة التطوير الشاملة والمحسّنة لمنصة ساري

> **تاريخ الإنشاء**: 19 ديسمبر 2025  
> **الإصدار**: 2.0 (محسّن ومطوّر)  
> **الحالة**: جاهز للتنفيذ

---

## 📊 **نظرة عامة**

### 🎯 **الهدف الاستراتيجي**

تحويل ساري من مساعد ذكي للمتاجر الإلكترونية فقط إلى **منصة شاملة** تدعم جميع أنواع الأعمال:

- ✅ **المتاجر الإلكترونية** (ملابس، إلكترونيات، مستحضرات)
- ✅ **مقدمي الخدمات** (عيادات، صالونات، استشارات، تدريب)
- ✅ **الشركات الصغيرة والمتوسطة** (مطاعم، ورش، مكاتب)
- ✅ **الأنشطة المختلطة** (متجر + خدمات)

---

## 🔍 **تحليل الخطة الأصلية**

### ✅ **نقاط القوة:**

1. **التدرج المنطقي**: البدء بـ Google Workspace (الأساسي) ثم الانتقال للأدوات المتقدمة
2. **التركيز على السوق السعودي**: إضافة بوابات دفع محلية (PayLink, Paymob)
3. **Setup Wizard**: تجربة مستخدم سلسة للتاجر الجديد
4. **المرونة**: دعم أنواع أعمال متعددة (متاجر، خدمات، مختلط)

### ⚠️ **نقاط التحسين:**

1. **ترتيب الأولويات**: Setup Wizard يجب أن يكون الأولوية رقم 1
2. **قاعدة البيانات**: تحتاج جداول إضافية للخدمات والمواعيد
3. **UX**: التدفق يحتاج تحسينات في التفاعل والمعاينة
4. **القوالب**: نحتاج نظام قوالب ذكي حسب نوع النشاط

---

## 🚀 **الخطة المحسّنة - إعادة ترتيب الأولويات**

### **المرحلة 0: Setup Wizard الأساسي** ⚡ (الأولوية القصوى)

**المدة المقدرة**: 3-4 أيام  
**الأهمية**: حرجة - بدونه لا يمكن للتاجر الجديد البدء

#### **المخرجات:**

- ✅ نظام إعداد تفاعلي من 8 خطوات
- ✅ اختيار نوع النشاط (متجر/خدمات/كلاهما)
- ✅ معلومات النشاط الأساسية
- ✅ إضافة المنتجات/الخدمات يدوياً
- ✅ تخصيص شخصية ساري (النبرة، اللغة، رسالة الترحيب)
- ✅ حفظ التقدم تلقائياً (Progress Saving)
- ✅ قوالب جاهزة حسب النوع (10 قوالب)
- ✅ معاينة مباشرة (Preview Mode)

#### **التدفق التفصيلي:**

```
الخطوة 1: مرحباً بك! 👋
├─ فيديو توضيحي قصير (30 ثانية)
├─ "ساري سيساعدك في إعداد كل شيء خلال 5 دقائق"
└─ زر "لنبدأ!" 🚀

الخطوة 2: ما نوع نشاطك؟
├─ [متجر إلكتروني] 🛍️
│   └─ أمثلة: ملابس، إلكترونيات، مستحضرات
├─ [خدمات] 💼
│   └─ أمثلة: عيادة، صالون، استشارات
├─ [كلاهما] 🎯
│   └─ مثال: صالون + منتجات تجميل
└─ شرح مختصر لكل نوع

الخطوة 3: اختر قالب جاهز (اختياري)
├─ 🦷 عيادة أسنان
├─ 💅 صالون تجميل
├─ 🍽️ مطعم
├─ 👕 متجر ملابس
├─ 📚 مكتبة
├─ 🏋️ نادي رياضي
├─ 🎓 مركز تدريب
├─ 🔧 ورشة صيانة
├─ ☕ مقهى
├─ 💼 استشارات مهنية
└─ [تخطي - سأعد كل شيء يدوياً]

الخطوة 4: معلومات نشاطك الأساسية
├─ اسم النشاط *
├─ رقم الواتساب * (مع اختيار مفتاح الدولة)
├─ ساعات العمل
│   ├─ [24/7]
│   ├─ [أيام العمل فقط] (السبت-الخميس)
│   └─ [مخصص] (اختيار أيام وأوقات محددة)
├─ العنوان (اختياري)
└─ وصف مختصر (اختياري)

الخطوة 5أ: للمتاجر - أضف منتجاتك
├─ [إضافة يدوي] ➕
│   ├─ اسم المنتج
│   ├─ السعر
│   ├─ الوصف
│   ├─ الصورة (رفع أو رابط)
│   └─ الكمية المتوفرة
├─ [رفع ملف CSV/Excel] 📄
│   ├─ تحميل الملف
│   ├─ تحليل ذكي للأعمدة
│   ├─ معاينة البيانات
│   └─ تأكيد الاستيراد
├─ [ربط مع Salla/Shopify] 🔗
│   └─ (مستقبلاً)
└─ معاينة المنتجات المضافة

الخطوة 5ب: للخدمات - أضف خدماتك
├─ اسم الخدمة *
├─ نوع السعر
│   ├─ [ثابت] (مثال: 150 ريال)
│   ├─ [متغير] (من X إلى Y)
│   └─ [حسب الطلب] (يُحدد بعد الاستشارة)
├─ المدة (بالدقائق) *
├─ هل تحتاج حجز موعد؟
│   ├─ [نعم] → ربط مع Google Calendar
│   └─ [لا] → خدمة فورية
├─ الموظف المسؤول (اختياري)
└─ [إضافة خدمة أخرى] ➕

الخطوة 6: التكاملات (اختياري - يمكن تخطيه)
├─ Google Calendar 📅
│   ├─ [ربط الآن]
│   ├─ شرح الفوائد
│   └─ [ربط لاحقاً]
├─ Google Sheets 📊
│   ├─ [ربط الآن]
│   ├─ شرح الفوائد
│   └─ [ربط لاحقاً]
└─ [تخطي - سأربطها من لوحة التحكم]

الخطوة 7: تخصيص شخصية ساري 🤖
├─ اختر النبرة
│   ├─ [محترف] 👔 (رسمي ومهني)
│   ├─ [ودود] 😊 (دافئ وقريب)
│   └─ [مرح] 🎉 (خفيف وممتع)
├─ اللغة المفضلة
│   ├─ [عربي فقط]
│   ├─ [إنجليزي فقط]
│   └─ [كلاهما] (ساري يكتشف تلقائياً)
├─ رسالة الترحيب المخصصة
│   └─ (نص قابل للتعديل مع اقتراح ذكي)
└─ معاينة مباشرة للمحادثة

الخطوة 8: جاهز للانطلاق! 🎉
├─ ملخص الإعداد
│   ├─ نوع النشاط: ✓
│   ├─ المنتجات/الخدمات: X عنصر
│   ├─ التكاملات: X مفعّل
│   └─ شخصية ساري: ✓
├─ [معاينة ساري الآن] 👀
│   └─ محادثة تجريبية كاملة
├─ [تجربة محادثة] 💬
│   └─ أمثلة جاهزة للتجربة
└─ [الذهاب للوحة التحكم] 🚀
```

---

### **المرحلة 1: Google Calendar Integration** 📅

**المدة المقدرة**: 4-5 أيام  
**الأهمية**: عالية جداً - أساسية لمقدمي الخدمات

#### **الميزات الأساسية:**

1. **حجز المواعيد من WhatsApp**
   - العميل يطلب موعد → ساري يعرض الأوقات المتاحة
   - اختيار الوقت → تأكيد فوري
   - إضافة الموعد في Google Calendar تلقائياً

2. **التذكيرات التلقائية**
   - تذكير قبل 24 ساعة (رسالة WhatsApp)
   - تذكير قبل 1 ساعة (رسالة WhatsApp)
   - قابل للتخصيص (التوقيت والمحتوى)

3. **إدارة المواعيد**
   - إلغاء الموعد (من العميل أو التاجر)
   - تعديل الموعد (تغيير الوقت)
   - عرض جميع المواعيد القادمة
   - سجل المواعيد السابقة

4. **منع الحجز المزدوج**
   - التحقق من التوفر قبل التأكيد
   - Buffer time بين المواعيد (قابل للتخصيص)
   - حد أقصى للمواعيد في اليوم

5. **دعم موظفين متعددين**
   - تعيين خدمة لموظف محدد
   - كل موظف له Google Calendar منفصل
   - عرض توفر كل موظف بشكل منفصل

#### **APIs المطلوبة:**

```typescript
// في server/routers.ts
calendar: {
  // ربط Google Calendar
  connect: protectedProcedure
    .input(z.object({ authCode: z.string() }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // الحصول على الأوقات المتاحة
  getAvailableSlots: protectedProcedure
    .input(z.object({ 
      serviceId: z.number(), 
      date: z.string(),
      staffId: z.number().optional() 
    }))
    .query(async ({ ctx, input }) => { ... }),
  
  // حجز موعد
  bookAppointment: protectedProcedure
    .input(z.object({
      serviceId: z.number(),
      customerPhone: z.string(),
      customerName: z.string(),
      appointmentDate: z.string(),
      startTime: z.string(),
      staffId: z.number().optional()
    }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // إلغاء موعد
  cancelAppointment: protectedProcedure
    .input(z.object({ appointmentId: z.number() }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // قائمة المواعيد
  listAppointments: protectedProcedure
    .input(z.object({ 
      status: z.enum(['upcoming', 'past', 'cancelled']).optional(),
      startDate: z.string().optional(),
      endDate: z.string().optional()
    }))
    .query(async ({ ctx, input }) => { ... }),
}
```

#### **الواجهات المطلوبة:**

1. **صفحة إعدادات Google Calendar** (`/merchant/integrations/calendar`)
   - زر "ربط Google Calendar"
   - عرض الحسابات المربوطة
   - إعدادات التذكيرات
   - إعدادات Buffer time

2. **صفحة المواعيد** (`/merchant/appointments`)
   - تقويم شهري/أسبوعي/يومي
   - قائمة المواعيد القادمة
   - فلاتر (حسب الخدمة، الموظف، الحالة)
   - إحصائيات (عدد المواعيد، معدل الإلغاء)

3. **تحديث صفحة الخدمات**
   - إضافة حقل "يتطلب موعد"
   - ربط الخدمة بموظف
   - تحديد مدة الخدمة

---

### **المرحلة 2: Google Sheets Integration** 📊

**المدة المقدرة**: 3-4 أيام  
**الأهمية**: عالية - أساسية لتتبع البيانات

#### **الميزات الأساسية:**

1. **حفظ الطلبات تلقائياً**
   - كل طلب جديد → صف جديد في Sheet
   - الحقول: التاريخ، اسم العميل، الهاتف، المنتجات، الإجمالي، الحالة

2. **تتبع العملاء المحتملين (Leads)**
   - كل محادثة جديدة → Lead جديد
   - الحقول: التاريخ، الاسم، الهاتف، الاهتمام، المرحلة، الملاحظات

3. **تصدير المحادثات**
   - تصدير جميع المحادثات إلى Sheet
   - فلترة حسب التاريخ/العميل
   - تحديث تلقائي (يومي/أسبوعي)

4. **التقارير التلقائية**
   - تقرير يومي: عدد الطلبات، الإيرادات، العملاء الجدد
   - تقرير أسبوعي: ملخص الأداء، أفضل المنتجات
   - تقرير شهري: تحليل شامل، مقارنات

5. **تحديث المخزون** (للخدمات البسيطة)
   - قراءة المنتجات من Sheet
   - تحديث الكميات تلقائياً بعد كل طلب
   - تنبيه عند نفاد المخزون

#### **APIs المطلوبة:**

```typescript
// في server/routers.ts
sheets: {
  // ربط Google Sheets
  connect: protectedProcedure
    .input(z.object({ authCode: z.string() }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // إنشاء Sheet جديد
  createSheet: protectedProcedure
    .input(z.object({ 
      sheetName: z.string(),
      type: z.enum(['orders', 'leads', 'conversations', 'inventory'])
    }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // تصدير البيانات
  exportData: protectedProcedure
    .input(z.object({
      sheetId: z.string(),
      dataType: z.enum(['orders', 'leads', 'conversations']),
      startDate: z.string().optional(),
      endDate: z.string().optional()
    }))
    .mutation(async ({ ctx, input }) => { ... }),
  
  // جدولة التقارير التلقائية
  scheduleReport: protectedProcedure
    .input(z.object({
      sheetId: z.string(),
      frequency: z.enum(['daily', 'weekly', 'monthly']),
      reportType: z.string()
    }))
    .mutation(async ({ ctx, input }) => { ... }),
}
```

#### **الواجهات المطلوبة:**

1. **صفحة إعدادات Google Sheets** (`/merchant/integrations/sheets`)
   - زر "ربط Google Sheets"
   - قائمة Sheets المربوطة
   - إعدادات التصدير التلقائي
   - جدولة التقارير

2. **صفحة التصدير** (`/merchant/export`)
   - اختيار نوع البيانات
   - فلاتر التاريخ
   - معاينة البيانات
   - زر "تصدير إلى Google Sheets"

---

### **المرحلة 3: نظام إدارة الخدمات المتقدم** 💼

**المدة المقدرة**: 3-4 أيام  
**الأهمية**: عالية - أساسية لمقدمي الخدمات

#### **الميزات الأساسية:**

1. **إدارة الخدمات (CRUD)**
   - إضافة خدمة جديدة
   - تعديل خدمة موجودة
   - حذف خدمة
   - تفعيل/تعطيل خدمة

2. **أنواع التسعير**
   - **ثابت**: سعر محدد (مثال: 150 ريال)
   - **متغير**: نطاق سعري (من 100 إلى 300 ريال)
   - **حسب الطلب**: يُحدد بعد الاستشارة

3. **إدارة الموظفين**
   - إضافة موظف جديد
   - ربط الموظف بخدمات محددة
   - جدول مواعيد لكل موظف
   - إحصائيات أداء الموظف

4. **حزم الخدمات (Packages)**
   - إنشاء حزمة من عدة خدمات
   - سعر مخفض للحزمة
   - عرض الحزم للعملاء

5. **تصنيفات الخدمات**
   - تنظيم الخدمات في فئات
   - تسهيل البحث والعرض

#### **الجداول المطلوبة:**

```sql
-- جدول الخدمات المحسّن
CREATE TABLE services (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(100),
  
  -- التسعير
  price_type ENUM('fixed', 'variable', 'custom') DEFAULT 'fixed',
  base_price DECIMAL(10, 2),
  min_price DECIMAL(10, 2),
  max_price DECIMAL(10, 2),
  
  -- الوقت
  duration_minutes INT NOT NULL,
  buffer_time_minutes INT DEFAULT 0,
  
  -- الحجز
  requires_appointment BOOLEAN DEFAULT true,
  max_bookings_per_day INT,
  advance_booking_days INT DEFAULT 30,
  
  -- الموظفين
  staff_ids JSON, -- [1, 2, 3]
  
  -- الحالة
  is_active BOOLEAN DEFAULT true,
  display_order INT DEFAULT 0,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);

-- جدول الموظفين
CREATE TABLE staff_members (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  email VARCHAR(255),
  role VARCHAR(100),
  
  -- إعدادات العمل
  working_hours JSON, -- {"monday": {"start": "09:00", "end": "17:00"}, ...}
  is_active BOOLEAN DEFAULT true,
  
  -- Google Calendar
  google_calendar_id VARCHAR(255),
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);

-- جدول حزم الخدمات
CREATE TABLE service_packages (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  
  service_ids JSON, -- [1, 2, 3]
  
  original_price DECIMAL(10, 2),
  package_price DECIMAL(10, 2),
  discount_percentage INT,
  
  is_active BOOLEAN DEFAULT true,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);

-- جدول تصنيفات الخدمات
CREATE TABLE service_categories (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  icon VARCHAR(50),
  display_order INT DEFAULT 0,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);
```

#### **الواجهات المطلوبة:**

1. **صفحة إدارة الخدمات** (`/merchant/services`)
   - قائمة الخدمات مع فلاتر
   - زر "إضافة خدمة جديدة"
   - تعديل/حذف خدمة
   - ترتيب الخدمات (drag & drop)

2. **صفحة إدارة الموظفين** (`/merchant/staff`)
   - قائمة الموظفين
   - إضافة/تعديل موظف
   - ربط الموظف بالخدمات
   - جدول مواعيد الموظف

3. **صفحة حزم الخدمات** (`/merchant/packages`)
   - قائمة الحزم
   - إنشاء حزمة جديدة
   - حساب السعر والخصم تلقائياً

---

### **المرحلة 4: نظام القوالب الجاهزة** 📋

**المدة المقدرة**: 2-3 أيام  
**الأهمية**: متوسطة - تسريع الإعداد للتاجر الجديد

#### **القوالب المطلوبة (10 قوالب):**

1. **🦷 عيادة أسنان**
   ```json
   {
     "business_type": "dental_clinic",
     "services": [
       { "name": "فحص عام", "price": 150, "duration": 30 },
       { "name": "تنظيف الأسنان", "price": 200, "duration": 45 },
       { "name": "حشو الأسنان", "price": 300, "duration": 60 },
       { "name": "تبييض الأسنان", "price": 800, "duration": 90 }
     ],
     "working_hours": {
       "saturday": { "start": "09:00", "end": "21:00" },
       "sunday": { "start": "09:00", "end": "21:00" },
       "monday": { "start": "09:00", "end": "21:00" },
       "tuesday": { "start": "09:00", "end": "21:00" },
       "wednesday": { "start": "09:00", "end": "21:00" },
       "thursday": { "start": "09:00", "end": "21:00" }
     },
     "bot_personality": {
       "tone": "professional",
       "welcome_message": "مرحباً! أنا ساري، مساعدك الذكي في عيادة الأسنان. كيف يمكنني مساعدتك اليوم؟"
     }
   }
   ```

2. **💅 صالون تجميل**
3. **🍽️ مطعم**
4. **👕 متجر ملابس**
5. **📚 مكتبة**
6. **🏋️ نادي رياضي**
7. **🎓 مركز تدريب**
8. **🔧 ورشة صيانة**
9. **☕ مقهى**
10. **💼 استشارات مهنية**

#### **الجدول المطلوب:**

```sql
CREATE TABLE business_templates (
  id INT PRIMARY KEY AUTO_INCREMENT,
  business_type VARCHAR(100) NOT NULL,
  template_name VARCHAR(255) NOT NULL,
  icon VARCHAR(50),
  
  -- البيانات الافتراضية
  services JSON,
  products JSON,
  working_hours JSON,
  bot_personality JSON,
  settings JSON,
  
  -- الوصف
  description TEXT,
  suitable_for TEXT,
  
  is_active BOOLEAN DEFAULT true,
  usage_count INT DEFAULT 0,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### **API المطلوب:**

```typescript
templates: {
  // قائمة القوالب
  list: publicProcedure
    .query(async () => { ... }),
  
  // تطبيق قالب
  apply: protectedProcedure
    .input(z.object({ templateId: z.number() }))
    .mutation(async ({ ctx, input }) => { ... }),
}
```

---

### **المرحلة 5: ميزات إضافية محسّنة** ✨

#### **1. Import Wizard للمنتجات**

**الميزات:**
- رفع ملف CSV/Excel (حتى 1000 منتج)
- تحليل ذكي للأعمدة (اسم، سعر، وصف، صورة، كمية)
- معاينة البيانات قبل الاستيراد
- معالجة الأخطاء تلقائياً (أسعار سالبة، حقول فارغة)
- تقرير الاستيراد (X نجح، Y فشل)

**الواجهة:**
```
صفحة /merchant/products/import
├─ تحميل الملف (CSV/Excel)
├─ تحليل الأعمدة
│   ├─ العمود A → اسم المنتج
│   ├─ العمود B → السعر
│   └─ العمود C → الوصف
├─ معاينة البيانات (أول 10 صفوف)
├─ تأكيد الاستيراد
└─ تقرير النتائج
```

#### **2. Preview Mode**

**الميزات:**
- تجربة ساري مباشرة قبل النشر
- محادثة تجريبية مع أمثلة
- اختبار الردود والتكاملات
- تعديل فوري للإعدادات

**الواجهة:**
```
نافذة منبثقة في Setup Wizard
├─ واجهة WhatsApp محاكاة
├─ أمثلة جاهزة للتجربة
│   ├─ "أريد حجز موعد"
│   ├─ "ما هي أسعاركم؟"
│   └─ "أريد طلب منتج"
├─ عرض رد ساري الفعلي
└─ زر "تعديل الإعدادات"
```

#### **3. Smart Suggestions**

**الميزات:**
- اقتراحات ذكية بناءً على نوع النشاط
- ملء تلقائي للحقول
- أمثلة واقعية

**مثال:**
```
عند اختيار "عيادة أسنان":
├─ اقتراح الخدمات:
│   ├─ فحص عام (150 ريال، 30 دقيقة)
│   ├─ تنظيف (200 ريال، 45 دقيقة)
│   └─ حشو (300 ريال، 60 دقيقة)
├─ اقتراح ساعات العمل:
│   └─ 9 صباحاً - 9 مساءً (السبت-الخميس)
└─ اقتراح رسالة الترحيب:
    └─ "مرحباً! أنا ساري، مساعدك في عيادة..."
```

#### **4. نظام الإشعارات الذكية**

**الميزات:**
- إشعار عند موعد جديد (للتاجر)
- إشعار عند إلغاء موعد
- إشعار عند وصول الحد الأقصى للباقة
- تقرير أسبوعي تلقائي (عبر WhatsApp أو البريد)

#### **5. نظام التقييمات**

**الميزات:**
- طلب تقييم بعد إتمام الخدمة
- حفظ التقييمات في قاعدة البيانات
- عرض متوسط التقييم في لوحة التحكم
- عرض التقييمات للعملاء الجدد

**الجدول:**
```sql
CREATE TABLE service_reviews (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  service_id INT NOT NULL,
  customer_phone VARCHAR(20) NOT NULL,
  customer_name VARCHAR(255),
  rating INT NOT NULL, -- 1-5
  comment TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE,
  FOREIGN KEY (service_id) REFERENCES services(id) ON DELETE CASCADE
);
```

#### **6. Multi-language Support**

**الميزات:**
- ساري يكتشف لغة العميل تلقائياً
- يرد بنفس اللغة (عربي/إنجليزي)
- إعدادات لغة قابلة للتخصيص

---

## 📋 **خطة التنفيذ التفصيلية (8 أسابيع)**

| الأسبوع | المرحلة | المهام الرئيسية | المخرجات المتوقعة |
|---------|---------|------------------|-------------------|
| **1** | Setup Wizard الأساسي | تصميم قاعدة البيانات + بناء 8 خطوات + القوالب | نظام إعداد كامل للتاجر الجديد |
| **2** | Google Calendar | APIs + ربط Google + حجز المواعيد + التذكيرات | نظام حجز مواعيد كامل |
| **3** | Google Sheets | APIs + ربط Google + تصدير البيانات + التقارير | نظام حفظ وتصدير البيانات |
| **4** | نظام الخدمات المتقدم | جداول الخدمات + الموظفين + الحزم + الواجهات | إدارة خدمات شاملة |
| **5** | القوالب الجاهزة | إنشاء 10 قوالب + API + دمج في Setup Wizard | 10 قوالب جاهزة للاستخدام |
| **6** | Import Wizard + Preview | رفع CSV + تحليل + معاينة + Preview Mode | استيراد منتجات + معاينة ساري |
| **7** | الإشعارات + التقييمات | نظام إشعارات ذكي + نظام تقييمات | ميزات إضافية محسّنة |
| **8** | الاختبارات + التوثيق | كتابة اختبارات شاملة + توثيق + Checkpoint | Checkpoint نهائي جاهز للنشر |

---

## 🗄️ **قاعدة البيانات - الجداول الجديدة**

### **1. جدول أنواع الأنشطة**

```sql
CREATE TABLE business_types (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name_ar VARCHAR(255) NOT NULL,
  name_en VARCHAR(255) NOT NULL,
  icon VARCHAR(50),
  description TEXT,
  default_settings JSON,
  suggested_services JSON,
  is_active BOOLEAN DEFAULT true,
  display_order INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### **2. جدول الخدمات المحسّن**

```sql
CREATE TABLE services (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(100),
  
  -- التسعير
  price_type ENUM('fixed', 'variable', 'custom') DEFAULT 'fixed',
  base_price DECIMAL(10, 2),
  min_price DECIMAL(10, 2),
  max_price DECIMAL(10, 2),
  
  -- الوقت
  duration_minutes INT NOT NULL,
  buffer_time_minutes INT DEFAULT 0,
  
  -- الحجز
  requires_appointment BOOLEAN DEFAULT true,
  max_bookings_per_day INT,
  advance_booking_days INT DEFAULT 30,
  
  -- الموظفين
  staff_ids JSON,
  
  -- الحالة
  is_active BOOLEAN DEFAULT true,
  display_order INT DEFAULT 0,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);
```

### **3. جدول حزم الخدمات**

```sql
CREATE TABLE service_packages (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  service_ids JSON,
  original_price DECIMAL(10, 2),
  package_price DECIMAL(10, 2),
  discount_percentage INT,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);
```

### **4. جدول تكاملات Google**

```sql
CREATE TABLE google_integrations (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  integration_type ENUM('calendar', 'sheets') NOT NULL,
  credentials TEXT, -- encrypted JSON
  calendar_id VARCHAR(255),
  sheet_id VARCHAR(255),
  is_active BOOLEAN DEFAULT true,
  last_sync TIMESTAMP,
  settings JSON,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE,
  UNIQUE KEY unique_integration (merchant_id, integration_type)
);
```

### **5. جدول المواعيد**

```sql
CREATE TABLE appointments (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  customer_phone VARCHAR(20) NOT NULL,
  customer_name VARCHAR(255),
  service_id INT NOT NULL,
  staff_id INT,
  
  appointment_date DATE NOT NULL,
  start_time TIME NOT NULL,
  end_time TIME NOT NULL,
  
  status ENUM('pending', 'confirmed', 'cancelled', 'completed', 'no_show') DEFAULT 'pending',
  
  google_event_id VARCHAR(255),
  reminder_24h_sent BOOLEAN DEFAULT false,
  reminder_1h_sent BOOLEAN DEFAULT false,
  
  notes TEXT,
  cancellation_reason TEXT,
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE,
  FOREIGN KEY (service_id) REFERENCES services(id) ON DELETE CASCADE,
  FOREIGN KEY (staff_id) REFERENCES staff_members(id) ON DELETE SET NULL
);
```

### **6. جدول القوالب الجاهزة**

```sql
CREATE TABLE business_templates (
  id INT PRIMARY KEY AUTO_INCREMENT,
  business_type VARCHAR(100) NOT NULL,
  template_name VARCHAR(255) NOT NULL,
  icon VARCHAR(50),
  services JSON,
  products JSON,
  working_hours JSON,
  bot_personality JSON,
  settings JSON,
  description TEXT,
  suitable_for TEXT,
  is_active BOOLEAN DEFAULT true,
  usage_count INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### **7. جدول الموظفين**

```sql
CREATE TABLE staff_members (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  name VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  email VARCHAR(255),
  role VARCHAR(100),
  working_hours JSON,
  is_active BOOLEAN DEFAULT true,
  google_calendar_id VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);
```

### **8. جدول التقييمات**

```sql
CREATE TABLE service_reviews (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  service_id INT NOT NULL,
  customer_phone VARCHAR(20) NOT NULL,
  customer_name VARCHAR(255),
  rating INT NOT NULL,
  comment TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE,
  FOREIGN KEY (service_id) REFERENCES services(id) ON DELETE CASCADE
);
```

### **9. جدول تقدم Setup Wizard**

```sql
CREATE TABLE setup_wizard_progress (
  id INT PRIMARY KEY AUTO_INCREMENT,
  merchant_id INT NOT NULL,
  current_step INT DEFAULT 1,
  completed_steps JSON, -- [1, 2, 3]
  wizard_data JSON, -- بيانات مؤقتة
  is_completed BOOLEAN DEFAULT false,
  completed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  FOREIGN KEY (merchant_id) REFERENCES merchants(id) ON DELETE CASCADE
);
```

---

## 🎨 **تحسينات UX/UI**

### **1. Setup Wizard**

- تصميم modern مع progress bar
- أيقونات واضحة لكل خطوة
- رسوم توضيحية (illustrations)
- رسائل تشجيعية ("رائع! خطوة واحدة أخرى فقط!")
- حفظ تلقائي مع إشعار ("تم الحفظ ✓")

### **2. لوحة التحكم**

- إضافة قسم "البدء السريع" للتجار الجدد
- نصائح ذكية حسب نوع النشاط
- إحصائيات مرئية (charts)
- إشعارات في الوقت الفعلي

### **3. صفحة المواعيد**

- تقويم تفاعلي (drag & drop)
- ألوان مختلفة لكل حالة
- فلاتر سريعة
- معاينة سريعة للموعد (hover)

---

## ✅ **معايير النجاح**

### **للمرحلة 0 (Setup Wizard):**
- ✅ 90% من التجار يكملون الإعداد خلال 5 دقائق
- ✅ معدل استخدام القوالب > 60%
- ✅ معدل التخلي < 10%

### **للمرحلة 1 (Google Calendar):**
- ✅ 80% من مقدمي الخدمات يربطون Google Calendar
- ✅ 0% حجز مزدوج
- ✅ معدل إرسال التذكيرات 100%

### **للمرحلة 2 (Google Sheets):**
- ✅ 70% من التجار يربطون Google Sheets
- ✅ تصدير البيانات خلال < 5 ثواني
- ✅ دقة البيانات 100%

---

## 📝 **ملاحظات مهمة**

1. **الأمان**: جميع بيانات Google يجب تشفيرها
2. **الأداء**: استخدام caching للبيانات المتكررة
3. **الاختبارات**: كتابة اختبارات شاملة لكل ميزة
4. **التوثيق**: توثيق كل API و Component
5. **Rollback**: إنشاء checkpoint بعد كل مرحلة

---

## 🚀 **الخطوة التالية**

**الآن جاهزون للبدء!** الأولوية القصوى:

### **المرحلة 0: Setup Wizard الأساسي**

**الخطوات:**
1. تصميم قاعدة البيانات (الجداول الجديدة)
2. إنشاء APIs الأساسية
3. بناء الواجهات (8 خطوات)
4. إضافة القوالب الجاهزة (10 قوالب)
5. كتابة الاختبارات
6. إنشاء Checkpoint

**المدة المتوقعة**: 3-4 أيام

---

**تم إنشاء هذا الملف بواسطة**: ساري AI Assistant  
**آخر تحديث**: 19 ديسمبر 2025
